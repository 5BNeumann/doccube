# Doccube

Agents should never be used on this project, I created it to try and use nim in a "real-world" context and have fun, agents defeat the whole purpose of it.

## Setup instructions

- On linux, run `rm -rf .`, on windows run `rd -r .`
